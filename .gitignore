node_modules/
artifacts/
cache/
types/
coverage/
coverage.json

# Ignition keeps deployed addresses here; journals are safe to commit, but the
# whole folder is ignored by default so nothing local leaks by accident.
ignition/deployments/
ignition/params.json

# Never commit keys.
.env
.env.local
*.key

# Source archives dropped in the root to be unpacked (nuraswap-contracts.zip and
# friends). The unpacked contracts are what belongs in git, not the tarball.
*.zip

# Editor / OS noise
.DS_Store
.idea/
.vscode/
